name: CI

on:
  push:
    branches: [ ci-cd-rauc ]
    tags:
      - '*'
  pull_request:
    branches: [ ci-cd-rauc ]
  workflow_dispatch:

jobs:
  build:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Run a command
        id: run-command
        run: |
          echo "Hello world" 
